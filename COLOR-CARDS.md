# 色卡数据与 API 迁移

预设目录共有 10 张，首次使用时“我的色卡”为空。用户从“添加色卡 → 品牌预设”查看并确认添加，也可新建自定义。浏览、刷新目录不会自动添加。创建流程必须明确选择色卡；没有色卡时停留在第 2 步。

| 色卡 | 颜色数 | 来源 / 边界 |
| --- | ---: | --- |
| Artkal M RGB | 220 | 官方 2025 PDF 的有效数值；2.6 mm，MH1 透明色暂无 RGB，未收录 |
| Artkal C RGB | 172 | 官方 2024 PDF 的有效数值；2.6 mm，特殊材质及异常 C152 未收录 |
| MARD | 291 | HansBug 社区整理，非官方标准；系列与尺寸未核实 |
| COCO | 291 | HansBug 社区整理，非官方标准；系列与尺寸未核实 |
| 漫漫 | 278 | HansBug 社区整理，包含上游色号修正，详见附带来源文档 |
| 咪小窝 | 286 | 社区参考；从 290 行中排除 4 个未知色号，不宣称品牌全色；系列和尺寸未核实 |
| 盼盼 | 285 | 社区参考；从 289 行中排除 4 个未知色号，不宣称品牌全色；系列和尺寸未核实 |
| Artkal M 体验 | 24 | 原有官方 RGB 数值选取样例，不是销售套装 |
| 基础配色 / 黑白灰 | 36 / 4 | 应用示例；D 编号不对应品牌色号 |

颜色名称沿用原色号，不编造官方中文名称。参考 RGB 用于确定性匹配与效果预览，不保证实物颜色复现。暂未增加透明/珠光等材质渲染。

## 数据追溯

- Artkal M：`https://cdn.shopify.com/s/files/1/1323/8195/files/M_MINI_Beads_RGB_Color_Chart_2025.pdf?v=1760661747`，直接提取并校验 220 组数值。
- Artkal C：`https://cdn.shopify.com/s/files/1/1323/8195/files/C_MINI_Beads_RGB_Color_Chart_2024.pdf?v=1744700289`，HansBug 数值子集与原 PDF 逐项比对一致。原 PDF 的 C152 为 189 / 199 / 273，273 无效，未自行猜测修正。
- 社区数据：`https://github.com/HansBug/pindou-color-data`，2026-05-19 数据快照；来源说明和 MIT 许可位于 `licenses/pindou-color-data/`。运行时也包含完整许可，可在色卡详情“数据许可”展开。
- 漫漫上游的 A20、A8、A9、G1、Q1 编号修正沿用整理者版本；不把社区标注当作品牌认证。
- 咪小窝和盼盼：逐项与上游 `https://git.xiongxiao.me/abearxiong/get-colors-from-beans/raw/branch/main/get-colors.json` 比对，收录数值全部一致；在 `https://github.com/Zippland/perler-beads/blob/master/src/app/colorSystemMapping.json` 中所有收录色号均有对应记录。跨来源的 RGB 并不相同，因此只核对色号身份，不混合另一表的 RGB。
- 两张新增参考卡均排除 UNKNOWN-01 至 UNKNOWN-04。记录来源 SHA-256、排除项及交叉覆盖数量于 `licenses/pindou-color-data/import-verification.json`；包含数据源原文和 MIT 许可。

## 分层

- `src/lib/color-cards.ts`：色卡实体、版本、品牌/系列/尺寸、来源、快照与匹配。
- `src/storage/preset-source.ts`：异步预设数据源接口、目录契约和运行时校验。当前实现为 `bundledPresetSource`。
- `src/storage/ColorCards.tsx`：分别向界面提供目录 `presets` 与本地用户集合 `cards`，处理加载、重试与失败回退；通过 `presetSource` 属性注入其他数据源。
- `src/storage/color-card-repository.ts`：用户色卡双快照存储，schema 2 支持预设快照及自定义；schema 1 仅迁移原自定义，不添加历史上自动显示的预设。
- 制作页与色卡管理页只访问 Provider，不直接读取内置文件、请求 URL 或解析服务端响应。

## 预留的 API 响应

```json
{
  "schemaVersion": 1,
  "catalogVersion": "2026-09-13.1",
  "cards": [
    {
      "id": "example-series-sample",
      "name": "样例色卡",
      "kind": "preset",
      "revision": 1,
      "brand": "示例品牌",
      "series": "示例系列",
      "size": "2.6 mm",
      "description": "部分颜色样例",
      "source": "来源说明",
      "sourceUrl": "",
      "colors": [{ "code": "A01", "name": "白色", "hex": "#ffffff" }]
    }
  ]
}
```

`PresetCardSource.load(signal?: AbortSignal): Promise<unknown>` 为接口边界；未来 HTTP 适配器负责 fetch、超时、认证（如需要）及传入 AbortSignal，返回解析后的 JSON。`loadPresetCatalog` 统一校验响应，不信任 TypeScript 类型断言。

在 App 的 `ColorCardsProvider` 传入 `presetSource={apiSource}` 即可更换数据来源，无需让页面知道服务器地址。当前没有配置服务器、联网拉取、后台刷新或远程缓存。

- `schemaVersion` 标识数据格式，`catalogVersion` 标识整个目录，`revision` 标识单张色卡。
- 同一色卡升级应保留稳定 `id` 并增加 `revision`。下架通过新目录不再返回该色卡表达。
- 不同品牌/系列/尺寸/套装使用不同的稳定 `id`，不能仅以色数作为身份。
- 只接受非空、编号唯一、每张含 1–512 色且 HEX 有效的预设目录；自定义 ID 使用独立的 `custom-` 命名空间。
- 来源链接只允许 HTTPS。错误响应保留当前内存中的有效目录；首次读取失败仍有内置样例。
- 数据源切换/卸载会取消旧请求；晚到响应不会覆盖新目录。
- 当前创建草稿保留被远程目录移除的选中色卡，不自动切换品牌。
- 正式接入 API 时补充：最近有效远程目录的磁盘缓存、HTTP 超时与重试策略、ETag 或增量更新。现在的回退只覆盖内置数据与当前会话内的有效数据。

## 图纸与自定义色卡

已保存 `Pattern.palette` 是完整颜色快照，新增的可选 `Pattern.colorCard` 保存色卡身份与来源。修改自定义色卡、更新/下架预设都不会改变旧图纸；旧版没有 `colorCard` 字段的图纸仍可读取。

自定义支持复制预设、勾选子集、从空白创建、手动添加色号/名称/HEX、切换“修改颜色”后轻点编辑（也保留长按入口）、修改品牌/系列/尺寸以及再次编辑已保存的色卡。保存失败会保留编辑草稿。暂未加入文件导入、删除色卡、库存或拥有状态。

CSV 每行包含色卡、品牌、系列、尺寸和版本；JSON 包含完整快照；PNG 标记色卡 ID 和版本，中文名称仍通过 CSV/JSON 导出。PNG 使用既有位图字库。

自动用色表示不额外压缩已匹配的颜色集合，上限仍受所选色卡范围限制。用户可以在“调整效果”中选择更低的用色上限。图纸需求量与完成进度独立于库存。

## 用户集合与更新策略

`addPreset` 只有在用户确认添加时才写入完整元数据和颜色快照；以稳定 ID 去重。重复添加不覆盖当前快照，目录更新或下架也不会覆盖/移除已添加色卡。首次目录失败可浏览内置回退，但仍需用户确认添加。存储错误不假装添加成功，关闭后可重试。以后可单独实现“更新已添加色卡”操作，本轮没有静默升级。

管理页可移除色卡；预设可再次添加，自定义移除有明确不可恢复提示。已有图纸始终保持自己的颜色和拼制进度。正在制作的草稿若色卡被移除，会保留当前选用快照并提示，避免中途切换品牌。

验证：43 项自动测试通过，包括 schema 1 迁移、空集合、去重、快照独立、损坏恢复，以及索引超过 255 的颜色在图纸生成/保存/CSV 中保持一致。iOS / Android / Web 资源导出通过；资源导出不等于原生安装包构建。

## 搜索交互

- 我的色卡与预设分别保留搜索词；我的色卡无匹配时，可携带关键词进入预设目录查找。
- 预设品牌快捷筛选与文本搜索叠加，咪小窝优先显示；清除搜索和品牌即可恢复列表。搜索绝不自动添加色卡。
- 支持名称、品牌、系列、尺寸、来源类别及色号；咪小窝支持 `mixiaowo` / `mxw` / `咪小窩` / `米小窝`，Artkal 支持优肯，COCO 支持可可。
- 空格组合搜索，例如 `mxw 77`；展示匹配色号和色块，点详情保留颜色筛选。
- 详情支持色号、颜色名称和 HEX；输入 77 不会误匹配 177。色号采用前缀匹配，完整色号优先；大小写和全角字符统一处理。
- 搜索只筛选展示，添加/复制时仍使用完整色卡；要自定义颜色范围，继续在复制编辑页明确勾选。
- 来源与许可在详情按需展开。输入搜索时临时收起说明和添加操作以留出空间，完成查找后恢复缺项说明、添加和复制按钮。
- iPhone SE / iOS 17 模拟器验证：我的色卡无结果转预设、`mxw 77` 查询及详情保留、清除恢复 286 色、屏幕键盘下完整显示色块和色号、完成查找后恢复操作。未新增用户色卡或图纸；本轮未复测 iPhone 真机。
