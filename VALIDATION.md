# 首版验证记录

- TypeScript：通过。
- 单元测试：48 项通过，0 项失败。含原有算法/色卡/进度测试，以及精确尺寸、留白/裁剪、512 格数据、导入校验、PNG 内存边界、SVG 转义、异步存储失败恢复。
- 静态构建：通过，输出约 1 MB（未压缩），离线缓存 11 个资源。
- Chrome 隔离移动视口：128×96 创建，咪小窝搜索/添加，完成标记与刷新恢复，撤销，SVG 下载，JSON 备份/导入去重，删除恢复，拖动、双触点缩放和锁定；512×512 创建和浏览均通过。
- Chrome 断网模拟：缓存应用重载、打开已存图纸通过。
- WebKit 26.5 隔离移动视口：创建、保存、导出、刷新、删除恢复、导入、拖动锁定与 512×512 图纸通过。
- WebKit 的自动化 `setOffline(true)` 导致内核内部错误，未记为通过。另启动独立 5174 服务，完成缓存后终止服务，缓存页面重载并继续示例创建通过。
- Worker 基准：256×256 合成源、291 色参考表、输出 512×512；本机 Chrome 约 485 ms，WebKit 约 543 ms。转换期间主线程计时器持续运行。不是 iPhone 性能结论。
- Expo：原移动端在 8082 保持运行，局域网 iOS manifest 返回 200，资源地址为 192.168.31.93:8082。
- Web：局域网 5173 返回 200。

尚未完成的环境验收：真实 iPhone Safari 相册选取、刘海与软键盘、iOS 主屏幕安装/独立窗口、真实断网重开。未配置 HTTPS 公网部署。请先使用局域网进行功能预览，再部署 HTTPS 后进行真机 PWA 验收。

端到端截图：work/create.png、work/dimensions.png、work/detail.png、work/large-chart.png。临时失败截图可能保留在 work/failure.png，仅用于排查，不代表最终状态。

## 页面缩放限制

- 已设置 viewport 缩放范围、允许滚动但禁用浏览器缩放的 touch-action，并拦截 Safari 原生 gesture 默认缩放。
- Chrome 实际双触点验证：移除 meta 缩放限制后，CSS 仍使 visualViewport.scale 保持不变。
- Chrome 端到端回归：画布双指缩放、拖动、创建和进度等通过。
- WebKit：viewport 配置和可取消 gesture 事件拦截通过；实际 iPhone 捏合仍需真机确认。

## 底部导航栏阴影修复（2026-09-24）

- TypeScript 与静态构建通过。
- Chrome 393×852 移动视口对比：原样式可复现矩形白边；外层与导航栏统一为 34px 圆角后，阴影贴合胶囊轮廓。
- 点击「我的」可正常切换页面；本次未进行 iPhone 真机复验。
